{
  "name": "artform",
  "version": "1.0.0",
  "description": "A self-host commission portal for creatives",
  "main": "index.js",
  "scripts": {
    "start:dev": "concurrently \"cd server && npm run start\" \"cd client && npm run dev\"",
    "install" : "concurrently \"cd server && npm install\" \"cd client && npm install\""
  },
  "author": "Nat Poulson",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
